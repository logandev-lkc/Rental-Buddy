# Rental Buddy 進度總表

最後更新：2026-05-09

## 目前產品結構

- 頂層頁面：`查核表`、`報告`
- 報告內檢視模式：
  - `手機版檢視`（友善閱讀）
  - `精簡版`（正式文件預覽）
- 匯出規則：`匯出文件（PDF）` 只輸出精簡版內容

## 已完成項目

### 1) 地圖與地址
- 查核表地址欄可開啟內嵌地圖，直接點選房源位置。
- 第一版使用 OpenStreetMap + Leaflet，不需要 Google API key。
- 點選地圖後會儲存 `latitude` / `longitude`，並用反查服務回填相似地址。
- 反查地址可再手動修正，避免免費服務地址格式不精準。

### 2) 核心資料與狀態
- 看房紀錄支援多筆，使用 `localStorage` 持久化。
- 每筆紀錄支援地址、月租金、各項 checklist 狀態與備註。
- 評分邏輯統一為 100 分制，並提供 A/B/C 評等。
- 評等門檻已調整為較嚴格版本：
  - A：85 以上
  - B：65-84
  - C：64 以下

### 3) 查核表體驗
- 新增月租金輸入欄位（與地址並列於基本資料區）。
- 紀錄切換、新增、刪除流程可用。
- 查核項可勾選/標記問題/展開補備註。
- 查核項支援快速狀態 chips 與高頻題目的細節選項 chips。
- 細節選項可自動同步 `checked` / `flagged` / `quickStatus`，降低現場操作步驟。

### 4) 報告頁體驗
- 改為「房屋評價」導向，不再使用性格測驗語氣。
- 報告頁可切換手機版檢視與精簡版預覽。
- 底欄改為兩大主按鈕（回到查核表、匯出 PDF）+ 可展開的「工具與 AI」區，減少一排過多按鈕。
- 手機版維持較溫馨、可快速掃描的資訊架構。

### 5) 精簡版與匯出
- 精簡版採檢驗單風格（分欄、表格、雷達、重點備註）。
- 匯出流程改為列印文件（非截圖），並鎖定輸出精簡版內容。
- 精簡版已加入 InBody 風格核心區塊：Rental Score、Confidence、分類分數、AI 分析總結、重要 Checklist 明細與下一步建議。
- 精簡版報表視覺再強化：表格與重要清單加入隔行底色、標題與欄位字距層級、下一步卡片圓角與留白優化，提升掃讀密度與穩定性。
- 精簡版在手機寬度的跑版問題已修正：
  - 單欄化
  - 雷達縮放
  - 表格可讀性調整
  - 操作按鈕避免擠壓

### 6) AI 報告整合
- 報告頁可複製完整 `reportData JSON`。
- 報告頁可複製一般 ChatGPT 報告生成 prompt。
- 報告頁可複製「可匯入 Prompt」，要求 ChatGPT 回傳固定 JSON。
- 可將 ChatGPT 回傳的 AI 分析 JSON 貼回前端，套用到 `AI 分析總結` 與 `下一步建議`。
- 若 AI JSON 缺漏、格式錯誤或被清除，報告仍會回到規則產生的 fallback。

### 7) 桌機版版面穩定性
- 調整桌機 header/footer/content 最大寬度與對齊。
- 改善捲動時覆蓋與視覺錯位問題。

### 8) PWA 與部署
- 已加入 PWA（manifest、icons、service worker）。
- 已修正 GitHub Pages 空白頁問題（`baseHref` 使用相對路徑）。
- 已同步 `docs/`，Pages 可部署最新版本。
- **F-010**：首次進入主流程後，符合條件時顯示「加到主畫面」提示；支援 Chromium 安裝對話框；iOS Safari 顯示「分享 → 加入主畫面」說明；提供「稍後」（約 7 天）與「不再顯示」（本機）。
- **F-011**：加入離線狀態提示列；離線顯示「資料仍保存在本機」與重新整理按鈕；恢復連線提示 4 秒後自動收合。
- service worker 快取策略微調：新增 OSM 圖磚與 Nominatim 反查快取群組，並加入「有新版本可用」提示與立即更新按鈕。
- docs 同步自動化：新增 GitHub Actions `Verify Docs Sync`，在 push/PR 自動執行 `npm run deploy:gh` 並檢查 `docs/` 是否有未提交差異；失敗時會直接提示修正指令與差異清單，且在 PR 自動留言修正步驟。

### 9) F-007 附件上傳（MVP S1）
- 查核表摘要新增「現場照片附件」區塊，可上傳圖片檔（本機 IndexedDB 儲存）。
- 每筆紀錄上限 10 張，並在 UI 顯示目前上傳數與剩餘可上傳數。
- 查核表顯示最多 3 張縮圖預覽，支援單張移除。
- 報告頁摘要區新增「附件照片」計數指標，提升決策資訊完整度。

### 10) F-007 附件上傳（MVP S2）
- 報告頁新增「現場附件照片」區塊，顯示最多 3 張縮圖與檔名，避免從查核跳到報告時資訊斷裂。
- 若附件超過 3 張，報告頁提示「另有 N 張」。
- 列印版報告 `print-meta` 新增附件總張數，讓 PDF 能交代是否有現場佐證資料。

### 11) F-007 附件上傳（MVP S3）
- 查核表附件縮圖可點擊放大，新增燈箱式預覽，方便快速檢查現場照片細節。
- 新增附件「多選刪除」模式，可勾選多張後一次刪除，降低清理成本。
- 切換紀錄時會自動清除附件多選與預覽狀態，避免跨紀錄操作誤刪。

### 12) F-007 附件上傳（MVP S4）
- 查核表附件新增排序控制（往前 / 往後），可調整顯示優先順序。
- 報告頁附件縮圖沿用同一排序，確保查核與報告呈現一致。

### 13) F-007 附件上傳（MVP S5／收尾）
- 列印版報告新增「現場附件照片」區塊，PDF 可輸出排序前三張縮圖與「另有 N 張」說明。
- 切換至報告頁與匯出 PDF 前會重新載入縮圖，降低列印區空白機率。
- 調整 production component style budget（56kB／65kB），反映目前 `app.css` 合理規模並消除建置警告。

### 14) F-009 Buy me a coffee（MVP）
- 查核表固定底欄與報告頁動作區底部新增中性文案外連「請作者喝杯咖啡」，新分頁開啟 Buy Me a Coffee（網址為程式常數，可替換為個人頁）。
- 進入主流程後才顯示（避免覆蓋 Intro）；查核表內容區底部留白略增，避免被加高的底欄遮擋。

### 15) F-013 查核表資訊架構與 UI 整合（收尾）
- `NEXT-FEATURES.md` 將 F-013 標為 **done**；`F-013-CHECKLIST-IA-DRAFT.md` §7 驗收項已勾選，§3 補充改版後走查說明。
- GitHub Actions `Verify Docs Sync`：PR 失敗留言中的 diff 改為 `<details>` 可展開區塊（完整 diff，逾約 58KB 字元時截斷並提示本機查看）。

## 規劃中（文件已對齊）

（目前無進行中的插入片段；後續需求請由 `NEXT-FEATURES.md` 新增列項。）

## 目前待辦（可排下一輪）

1. （可選）擴充 docs CI：於 PR 留言附加 artifacts 或更細的截斷策略（目前已為可展開 diff）。

## 開發與部署注意

- 開發指令：`npm start`（已改為 polling 監看，降低 `EMFILE` 影響）。
- 正式建置：`npm run build`
- GitHub Pages：目前使用 `docs/` 產物部署，推送時需要同步 `docs/`。
